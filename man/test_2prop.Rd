% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_2prop.R
\name{test_2prop}
\alias{test_2prop}
\title{2-Sample Proportions Hypothesis Test}
\usage{
test_2prop(
  success_1 = NULL,
  n_1 = NULL,
  success_2 = NULL,
  n_2 = NULL,
  group_names = NULL,
  formula = NULL,
  data = NULL,
  success_level = NULL,
  alternative = "two.sided",
  method = "theory",
  sim_reps = 1000
)
}
\arguments{
\item{success_1}{A whole number representing the count of successes in
Group 1. For example, if 45 out of 100 patients in the treatment group
recovered, \code{success_1 = 45}.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{n_1}{A whole number representing the total sample size of Group 1.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{success_2}{A whole number representing the count of successes in
Group 2. For example, if 25 out of 100 patients in the placebo group
recovered, \code{success_2 = 25}.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{n_2}{A whole number representing the total sample size of Group 2.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{group_names}{A character vector of length 2 giving meaningful names
to the two groups. For example, \code{group_names = c("Treatment", "Placebo")}.
Defaults to \code{c("Group 1", "Group 2")}. When using raw data, group names
are extracted automatically from the grouping variable.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{formula}{A two-sided formula of the form \code{Response ~ Group} that
identifies the response (categorical outcome) variable and the grouping
variable in your dataset. For example, \code{formula = Recovered ~ Treatment}
where \code{Recovered} is the outcome and \code{Treatment} defines the two groups.
\strong{Only used when NOT providing summary statistics.}}

\item{data}{A data frame (loaded from a \code{.csv}, \code{.txt}, or \code{.xlsx} file)
that contains the variables named in \code{formula}.
\strong{Only used when NOT providing summary statistics.}}

\item{success_level}{A character string specifying which value of the
response variable counts as a "success". For example, if the response
variable contains \code{"Yes"} and \code{"No"}, use \code{success_level = "Yes"}.
This argument is \strong{required} when using \code{formula} and \code{data}.}

\item{alternative}{The direction of the alternative hypothesis
(H\eqn{_a}). Must be one of:
\describe{
\item{\code{"two.sided"}}{H\eqn{_a}: \eqn{\pi_1 - \pi_2 \neq 0} (default)
-- use when testing if the two proportions are simply \emph{different}.}
\item{\code{"greater"}}{H\eqn{_a}: \eqn{\pi_1 - \pi_2 > 0} --
use when testing if Group 1's proportion is \emph{greater than} Group 2's.}
\item{\code{"less"}}{H\eqn{_a}: \eqn{\pi_1 - \pi_2 < 0} --
use when testing if Group 1's proportion is \emph{less than} Group 2's.}
}}

\item{method}{The method used to calculate the p-value. Must be one of:
\describe{
\item{\code{"theory"}}{(default) Uses the traditional two-proportion Z-test
with a pooled proportion. Appropriate when all four cells in the 2x2
contingency table have at least 10 observations.}
\item{\code{"simulation"}}{Uses a Randomization Test (Shuffling the Deck).
All successes and failures are pooled together and repeatedly
re-dealt into the two group sizes, simulating the null hypothesis
of no difference. More flexible than theory when cell counts are
small.}
}}

\item{sim_reps}{The number of randomization shuffles when
\code{method = "simulation"}. Default is \code{1000}. Increasing to \code{5000} gives
a more stable p-value estimate.}
}
\value{
An S3 object of class \code{stat218_2prop} containing all computed
values. You can use the following methods on the result:
\describe{
\item{\code{print(result)}}{Displays a clean summary including both sample
proportions, the observed difference, SD of null distribution, test
statistic, and p-value.}
\item{\code{plot(result)}}{Shows a beautified 2x2 contingency table
alongside the null distribution plot with shaded p-value region(s)
and SD of Null Distribution annotated.}
\item{\code{plot_steps(result)}}{Displays a detailed three-panel
step-by-step interpretation with proper fraction notation.}
}
}
\description{
Tests whether two population proportions are equal
(\eqn{\pi_1 = \pi_2}) using either a simulation-based approach
(Randomization Test) or a theory-based Z-test using a pooled proportion.

This function accepts input in \strong{two ways}:
\itemize{
\item \strong{Summary Statistics:} You already know the number of successes and
sample sizes for both groups (common in textbook homework problems).
Pass \code{success_1}, \code{n_1}, \code{success_2}, and \code{n_2} directly.
\item \strong{Raw Data:} You have an actual dataset loaded into R (common in
activities and projects). Pass \code{formula}, \code{data}, and \code{success_level}
instead, and the function will extract the group counts for you.
}
}
\details{
\subsection{The Core Idea}{

In a two-sample proportion test, we ask: \emph{"Is the difference in proportions
we observed between two groups consistent with random chance, or is there
real evidence that the two population proportions are different?"}

The null hypothesis always takes the form
H\eqn{_0}: \eqn{\pi_1 - \pi_2 = 0}, meaning we assume no difference
between the two population proportions.
}

\subsection{Simulation Method (Randomization Test)}{

We simulate the null hypothesis by pooling all successes and failures
together and repeatedly shuffling them back into the two group sizes.
Each shuffle gives a new simulated difference in proportions. The SD of
that simulated null distribution goes in the denominator of the test
statistic. The p-value is the proportion of shuffles that produced a
difference as extreme as or more extreme than what we observed.
}

\subsection{Theory Method (Pooled Z-Test)}{

Because the null hypothesis assumes \eqn{\pi_1 = \pi_2}, we estimate the
common proportion using a \strong{pooled proportion}:
\deqn{\hat{p} = \frac{\text{success}_1 + \text{success}_2}{n_1 + n_2}}
This pooled proportion is then used to compute the SD of the null
distribution:
\deqn{SD_{null} = \sqrt{\hat{p}(1-\hat{p})\left(\frac{1}{n_1} +
\frac{1}{n_2}\right)}}
}

\subsection{Validity Conditions for Theory Method}{

The theory-based Z-test is reliable when \strong{all four cells} in the 2x2
contingency table have at least 10 observations:
\itemize{
\item Successes in Group 1 \eqn{\geq 10}
\item Failures in Group 1 \eqn{\geq 10}
\item Successes in Group 2 \eqn{\geq 10}
\item Failures in Group 2 \eqn{\geq 10}
}

A warning is issued automatically if any cell falls below 10.
}
}
\examples{
# --- Summary Statistics Path (two-sided, theory) ---
# A clinical trial: 45 of 100 in treatment recovered vs 25 of 100 placebo.
result <- test_2prop(
  success_1 = 45, n_1 = 100,
  success_2 = 25, n_2 = 100,
  group_names = c("Treatment", "Placebo"),
  alternative = "two.sided", method = "theory"
)
print(result)
\dontrun{
plot(result)
plot_steps(result)
}

# --- Summary Statistics Path (one-sided, simulation) ---
# Testing if Group A has a higher success rate than Group B.
result2 <- test_2prop(
  success_1 = 38, n_1 = 80,
  success_2 = 29, n_2 = 80,
  group_names = c("Group A", "Group B"),
  alternative = "greater", method = "simulation"
)
print(result2)
\dontrun{
plot(result2)
plot_steps(result2)
}

# --- Raw Data Path (two-sided, theory) ---
# Using mtcars: does the proportion of manual cars differ by engine type?
car_data <- mtcars
car_data$transmission <- ifelse(mtcars$am == 1, "Manual", "Automatic")
car_data$engine <- ifelse(mtcars$vs == 1, "Straight", "V-shaped")
result3 <- test_2prop(
  formula = transmission ~ engine, data = car_data,
  success_level = "Manual", alternative = "two.sided",
  method = "theory"
)
print(result3)
\dontrun{
plot(result3)
plot_steps(result3)
}

# --- Raw Data Path (one-sided, simulation) ---
result4 <- test_2prop(
  formula = transmission ~ engine, data = car_data,
  success_level = "Manual", alternative = "greater",
  method = "simulation"
)
print(result4)
\dontrun{
plot(result4)
plot_steps(result4)
}

}
