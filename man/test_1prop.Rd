% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_1prop.R
\name{test_1prop}
\alias{test_1prop}
\title{1-Sample Proportion Hypothesis Test}
\usage{
test_1prop(
  successes = NULL,
  n = NULL,
  formula = NULL,
  data = NULL,
  success_level = NULL,
  null_pi = 0.5,
  alternative = "two.sided",
  method = "simulation",
  sim_reps = 1000
)
}
\arguments{
\item{successes}{A whole number representing the count of "successes"
(i.e., the number of times the outcome of interest was observed) in your
sample. For example, if 34 out of 80 students passed, \code{successes = 34}.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{n}{A whole number representing the total sample size (how many
observations were collected in total). For example, if you surveyed
80 students, \code{n = 80}.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{formula}{A one-sided formula of the form \code{~ variable} that identifies
the categorical variable in your dataset to analyze. For example,
\code{formula = ~ Passed} where \code{Passed} is a column in your data frame.
\strong{Only used when NOT providing \code{successes} and \code{n}.}}

\item{data}{A data frame (loaded from a \code{.csv}, \code{.txt}, or \code{.xlsx} file)
that contains the variable named in \code{formula}.
\strong{Only used when NOT providing \code{successes} and \code{n}.}}

\item{success_level}{A character string specifying which value of your
categorical variable counts as a "success". For example, if your variable
contains \code{"Yes"} and \code{"No"}, use \code{success_level = "Yes"}. This argument
is \strong{required} when using \code{formula} and \code{data}.}

\item{null_pi}{The hypothesized population proportion under the null
hypothesis (H\eqn{_0}). This is the value you are testing your sample
against. Must be a number strictly between 0 and 1. Default is \code{0.5}.}

\item{alternative}{The direction of the alternative hypothesis
(H\eqn{_a}). Must be one of:
\describe{
\item{\code{"two.sided"}}{H\eqn{_a}: \eqn{\pi \neq} \code{null_pi} (default) --
use when testing if the proportion is \emph{different from} the null.}
\item{\code{"greater"}}{H\eqn{_a}: \eqn{\pi >} \code{null_pi} --
use when testing if the proportion is \emph{greater than} the null.}
\item{\code{"less"}}{H\eqn{_a}: \eqn{\pi <} \code{null_pi} --
use when testing if the proportion is \emph{less than} the null.}
}}

\item{method}{The method used to calculate the p-value. Must be one of:
\describe{
\item{\code{"simulation"}}{Uses a simulation-based approach (The Great
Shuffle). Recommended as the default and most intuitive approach for
intro stats. The null distribution is built by repeatedly drawing
random samples under the assumption that H\eqn{_0} is true.}
\item{\code{"theory"}}{Uses the traditional Z-test formula. Appropriate when
validity conditions are met: at least 10 expected successes and 10
expected failures under the null (i.e., \eqn{n \cdot \pi_0 \geq 10}
and \eqn{n \cdot (1 - \pi_0) \geq 10}).}
}}

\item{sim_reps}{The number of simulated shuffles to run when
\code{method = "simulation"}. More repetitions give a more stable p-value
estimate. Default is \code{1000}. Increasing to \code{5000} or \code{10000} is
reasonable for final analyses.}
}
\value{
An S3 object of class \code{stat218_1prop} containing all computed
values. You can use the following methods on the result:
\describe{
\item{\code{print(result)}}{Displays a clean summary of the test.}
\item{\code{plot(result)}}{Plots the null distribution with shaded p-value
region, p-value annotation on the shaded area(s), and the SD of the
Null Distribution. Use \code{plot(result, plot_type = "dotplot")} for a
dot plot (simulation only).}
\item{\code{plot_steps(result)}}{Displays a detailed three-panel
step-by-step interpretation with proper fraction notation.}
}
}
\description{
Tests a claim about a single population proportion (\eqn{\pi}) using either
a simulation-based approach (The Great Shuffle) or a theory-based Z-test.

This function accepts input in \strong{two ways}:
\itemize{
\item \strong{Summary Statistics:} You already know the number of successes and the
sample size (common in textbook homework problems where the data is
summarized for you). Pass \code{successes} and \code{n} directly.
\item \strong{Raw Data:} You have an actual dataset loaded into R (common in
activities and projects). Pass \code{formula} and \code{data} instead, and the
function will count the successes for you.
}
}
\details{
\subsection{The Core Idea}{

In a one-sample proportion test, we are asking: \emph{"Is the proportion we
observed in our sample consistent with some claimed population proportion,
or is it too far away to be explained by random chance alone?"}

The null hypothesis always takes the form H\eqn{_0}: \eqn{\pi =} \code{null_pi},
where \eqn{\pi} (Greek letter "pi") represents the \strong{true, unknown
population proportion}.
}

\subsection{Simulation Method (The Great Shuffle)}{

We simulate what the world would look like \emph{if the null hypothesis were
true} by repeatedly drawing samples of size \code{n} from a population where
the true proportion is exactly \code{null_pi}. The SD of that simulated null
distribution goes in the denominator of the test statistic. The p-value
is the proportion of simulated samples as extreme as or more extreme than
our observed \eqn{\hat{p}}.
}

\subsection{Theory Method (Z-Test)}{

We standardize our observed \eqn{\hat{p}} using the formula:
\deqn{Z = \frac{\hat{p} - \pi_0}{SD_{null}}, \quad \text{where }
SD_{null} = \sqrt{\frac{\pi_0(1-\pi_0)}{n}}}
The SD of the null distribution is computed from the formula rather than
simulated -- but it represents the exact same concept.
}

\subsection{Validity Conditions for Theory Method}{

The theory-based Z-test is only reliable when:
\itemize{
\item \eqn{n \cdot \pi_0 \geq 10} (at least 10 expected successes under H\eqn{_0})
\item \eqn{n \cdot (1 - \pi_0) \geq 10} (at least 10 expected failures under H\eqn{_0})
}

If these are not met, use \code{method = "simulation"} instead.
}
}
\examples{
# --- Summary Statistics Path ---
# A claim that 50\% of students prefer online learning.
# In a sample of 80 students, 52 said yes.
result <- test_1prop(successes = 52, n = 80, null_pi = 0.5,
                     alternative = "two.sided", method = "theory")
print(result)
plot(result)
\dontrun{
plot_steps(result)
}

# --- Raw Data Path ---
# Using mtcars: is the proportion of manual transmission cars different from 50\%?
car_data <- mtcars
car_data$transmission <- ifelse(mtcars$am == 1, "Manual", "Automatic")
result2 <- test_1prop(formula = ~ transmission, data = car_data,
                      success_level = "Manual", null_pi = 0.5,
                      alternative = "two.sided", method = "simulation")
print(result2)
plot(result2)
\dontrun{
plot_steps(result2)
}

}
