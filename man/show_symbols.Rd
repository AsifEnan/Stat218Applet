% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/show_symbols.R
\name{show_symbols}
\alias{show_symbols}
\title{Show Statistical Symbol Reference Plots}
\usage{
show_symbols(topic = "all")
}
\arguments{
\item{topic}{A character string specifying which symbol plot to display.
Must be one of:
\describe{
\item{\code{"all"}}{Renders all five plots in sequence (default).}
\item{\code{"proportions"}}{Symbols related to proportions (pi, p-hat, etc.).}
\item{\code{"means"}}{Symbols related to means and quantitative variables.}
\item{\code{"tests"}}{Test statistics, decision-making symbols (Z, T, alpha, p-value).}
\item{\code{"regression"}}{Symbols for correlation and simple linear regression.}
\item{\code{"mistakes"}}{Common symbol mistakes students make in STAT 218.}
}}
}
\value{
Invisibly returns \code{NULL}. The function is called for its side effect
of rendering plots to the active graphics device.
}
\description{
Renders one or more educational reference plots that display the statistical
symbols used throughout STAT 218, organized by topic. Symbols are presented
in a two-column layout contrasting \strong{Population (Greek/Parameter)} symbols
on the left with \strong{Sample (Latin/Statistic)} symbols on the right.

When called with no arguments, all five plots are rendered sequentially into
the RStudio plot viewer — use the \strong{left/right arrow buttons} in the Plot
tab to scroll between them.
}
\details{
\subsection{The Core Idea: Greek vs. Latin}{

In statistics, \strong{Greek letters} (mu, sigma, pi, rho, beta) refer to
\strong{population parameters} — true values that are usually unknown. \strong{Latin
(English) letters} (x-bar, s, p-hat, r, b) refer to \strong{sample statistics}
— values we actually calculate from our data.
}

\subsection{The Five Themes}{
\enumerate{
\item \strong{Proportions} — categorical data, success/failure outcomes
\item \strong{Means} — quantitative data, averages and spread
\item \strong{Test Statistics} — standardized statistics and decision rules
\item \strong{Regression} — linear relationships between two quantitative variables
\item \strong{Common Mistakes} — frequent symbol confusions to avoid
}
}
}
\examples{
# Show all symbol plots (scroll with arrows in RStudio Plot tab)
show_symbols()

# Show only the proportions reference
show_symbols("proportions")

# Show only the means reference
show_symbols("means")

# Show the common mistakes plot
show_symbols("mistakes")

}
\seealso{
The main inference functions this package provides:
\code{\link[=test_1prop]{test_1prop()}}, \code{\link[=ci_1prop]{ci_1prop()}}, \code{\link[=test_2prop]{test_2prop()}}, \code{\link[=ci_2prop]{ci_2prop()}},
\code{\link[=test_1mean]{test_1mean()}}, \code{\link[=ci_1mean]{ci_1mean()}}, \code{\link[=test_2mean]{test_2mean()}}, \code{\link[=ci_2mean]{ci_2mean()}},
\code{\link[=test_paired]{test_paired()}}, \code{\link[=ci_paired]{ci_paired()}},
\code{\link[=test_correlation]{test_correlation()}}, \code{\link[=test_regression]{test_regression()}}
}
