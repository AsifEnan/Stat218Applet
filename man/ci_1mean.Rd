% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_1mean.R
\name{ci_1mean}
\alias{ci_1mean}
\title{1-Sample Mean Confidence Interval}
\usage{
ci_1mean(
  x_bar = NULL,
  n = NULL,
  sd_val = NULL,
  sd_type = "sample",
  formula = NULL,
  data = NULL,
  conf_level = 0.95,
  method = "2SD",
  sim_reps = 1000
)
}
\arguments{
\item{x_bar}{The observed sample mean-the average value from your sample.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{n}{A whole number representing the total sample size.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{sd_val}{The standard deviation. Whether this is a population or
sample SD is determined by \code{sd_type}.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{sd_type}{A character string specifying whether the standard deviation
is from the population or the sample. Must be one of:
\describe{
\item{\code{"sample"}}{(default) The SD was calculated from your sample
data. When \code{method = "theory"}, a T-test multiplier is used.}
\item{\code{"population"}}{The true population SD (\eqn{\sigma}) is known.
When \code{method = "theory"}, a Z multiplier is used.}
}
When raw data is provided via \code{formula} and \code{data}, this is always
set to \code{"sample"} automatically.}

\item{formula}{A one-sided formula of the form \code{~ variable} identifying
the numeric variable in your dataset. For example, \code{formula = ~ Height}.
\strong{Only used when NOT providing \code{x_bar}, \code{n}, and \code{sd_val}.}}

\item{data}{A data frame (loaded from a \code{.csv}, \code{.txt}, or \code{.xlsx} file)
containing the variable named in \code{formula}.
\strong{Only used when NOT providing summary statistics.}}

\item{conf_level}{The desired confidence level as a decimal. Default is
\code{0.95} for a 95\% confidence interval. Common choices are \code{0.90},
\code{0.95}, and \code{0.99}. Note that \code{method = "2SD"} only works at \code{0.95}.}

\item{method}{The method used to construct the confidence interval.
Must be one of:
\describe{
\item{\code{"2SD"}}{(default) The 2SD Simulation Method. A parametric
bootstrap distribution is generated centered at \code{x_bar}, and the
interval is constructed as \eqn{\bar{x} \pm 2 \times SD(\bar{x})}.
This method is only valid for 95\% confidence intervals. If you
need a different confidence level, use \code{method = "simulation"}.}
\item{\code{"simulation"}}{The Bootstrap Simulation Method. Same
parametric bootstrap as 2SD, but uses the middle \code{conf_level}\% of
the simulated distribution to find the bounds. Works for any
confidence level.}
\item{\code{"theory"}}{The Theory-Based Method. Uses the formula
\eqn{SE = s/\sqrt{n}} (or \eqn{\sigma/\sqrt{n}}) and the
appropriate Z* or t* multiplier. Works for any confidence level.
Requires validity conditions to be met.}
}}

\item{sim_reps}{The number of bootstrap samples when \code{method = "2SD"} or
\code{method = "simulation"}. Default is \code{1000}. Increasing to \code{5000} gives
a more stable interval.}
}
\value{
An S3 object of class \code{stat218_1mean_ci} containing all computed
values. You can use the following methods on the result:
\describe{
\item{\code{print(result)}}{Displays a clean summary of the interval.}
\item{\code{plot(result)}}{Shows the bootstrap or theoretical distribution
with the confidence interval shaded and labeled, plus a forest
plot of the interval below.}
\item{\code{plot_steps(result)}}{Displays a detailed three-panel
step-by-step construction of the interval.}
}
}
\description{
Constructs a confidence interval for a single population mean (\eqn{\mu})
using one of three methods: the 2SD simulation method, a general
simulation method, or a theory-based approach.

This function accepts input in \strong{two ways}:
\itemize{
\item \strong{Summary Statistics:} You already know the sample mean, standard
deviation, and sample size. Pass \code{x_bar}, \code{sd_val}, and \code{n} directly.
\item \strong{Raw Data:} You have an actual dataset loaded into R. Pass \code{formula}
and \code{data} instead, and the function computes all summary statistics
automatically.
}
}
\details{
\subsection{The Core Idea}{

A confidence interval gives a range of plausible values for the true
population mean \eqn{\mu}. Rather than giving a single point estimate
(which is almost certainly not exactly right), a confidence interval
acknowledges uncertainty and says: \emph{"We are X\% confident the true mean
falls somewhere in this range."}
}

\subsection{The 2SD Method}{

The book introduces this as the primary simulation-based approach for
95\% confidence intervals. We simulate the sampling distribution of the
sample mean by drawing many bootstrap samples, then use
\eqn{\bar{x} \pm 2 \times SD} of that distribution as our interval.
The multiplier of 2 is an approximation of the true 1.96 used in theory.
}

\subsection{Theory Method}{

Uses the formula SE = s/\eqn{\sqrt{n}} (or \eqn{\sigma}/\eqn{\sqrt{n}}
if the population SD is known), multiplied by the appropriate critical
value from the Z or T distribution. Requires:
\itemize{
\item \eqn{n \geq 20}, \strong{or}
\item The underlying distribution is roughly symmetric.
}
}
}
\examples{
# --- Summary Statistics (2SD method, default) ---
result <- ci_1mean(x_bar = 72.4, sd_val = 8.1, n = 35)
print(result)
\dontrun{
plot(result)
plot_steps(result)
}

# --- Summary Statistics (theory, T-interval) ---
result2 <- ci_1mean(x_bar = 72.4, sd_val = 8.1, n = 35,
                    sd_type = "sample", conf_level = 0.95,
                    method = "theory")
print(result2)
\dontrun{
plot(result2)
plot_steps(result2)
}

# --- Summary Statistics (simulation, 90\% CI) ---
result3 <- ci_1mean(x_bar = 72.4, sd_val = 8.1, n = 35,
                    conf_level = 0.90, method = "simulation")
print(result3)
\dontrun{
plot(result3)
plot_steps(result3)
}

# --- Raw Data (2SD method) ---
result4 <- ci_1mean(formula = ~ mpg, data = mtcars, method = "2SD")
print(result4)
\dontrun{
plot(result4)
plot_steps(result4)
}

# --- Raw Data (theory, T-interval) ---
result5 <- ci_1mean(formula = ~ mpg, data = mtcars, method = "theory")
print(result5)
\dontrun{
plot(result5)
plot_steps(result5)
}

}
