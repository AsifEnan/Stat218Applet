% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_2prop.R
\name{ci_2prop}
\alias{ci_2prop}
\title{2-Sample Proportions Confidence Interval}
\usage{
ci_2prop(
  success_1 = NULL,
  n_1 = NULL,
  success_2 = NULL,
  n_2 = NULL,
  group_names = NULL,
  formula = NULL,
  data = NULL,
  success_level = NULL,
  conf_level = 0.95,
  method = "2SD",
  sim_reps = 1000
)
}
\arguments{
\item{success_1}{A whole number representing the count of successes in
Group 1. For example, if 45 out of 100 patients recovered,
\code{success_1 = 45}.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{n_1}{The sample size of Group 1.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{success_2}{A whole number representing the count of successes in
Group 2.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{n_2}{The sample size of Group 2.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{group_names}{A character vector of length 2 giving meaningful names
to the two groups. For example,
\code{group_names = c("Treatment", "Control")}.
Defaults to \code{c("Group 1", "Group 2")}. When using raw data, group
names are extracted automatically from the grouping variable.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{formula}{A two-sided formula of the form \code{Response ~ Group}
identifying the categorical response variable and the grouping
variable in your dataset. For example,
\code{formula = Recovered ~ Treatment}.
\strong{Only used when NOT providing summary statistics.}}

\item{data}{A data frame (loaded from a \code{.csv}, \code{.txt}, or \code{.xlsx} file)
containing the variables named in \code{formula}.
\strong{Only used when NOT providing summary statistics.}}

\item{success_level}{A character string specifying which value of the
response variable counts as a "success". For example, if the response
variable contains \code{"Yes"} and \code{"No"}, use \code{success_level = "Yes"}.
\strong{Required when using \code{formula} and \code{data}.}}

\item{conf_level}{The desired confidence level as a decimal. Default is
\code{0.95} for a 95\% confidence interval. Common choices are \code{0.90},
\code{0.95}, and \code{0.99}. Note that \code{method = "2SD"} only works at \code{0.95}.}

\item{method}{The method used to construct the confidence interval.
Must be one of:
\describe{
\item{\code{"2SD"}}{(default) The 2SD Simulation Method. An independent bootstrap is generated by resampling with replacement from each group separately.The interval
is \eqn{(\hat{p}_1 - \hat{p}_2) \pm 2 \times SD}. Only valid for 95\% confidence interval limit. If you
need a different confidence level, use \code{method = "simulation"}.}
\item{\code{"simulation"}}{The Bootstrap Simulation Method. Same
independent bootstrap as 2SD, but uses the middle \code{conf_level}\% of
the simulated distribution to find the bounds. Works for any
confidence level.}
\item{\code{"theory"}}{The Theory-Based Method. Uses the unpooled
standard error formula and the appropriate Z* multiplier.
Works for any confidence level and with both summary statistics
and raw data.}
}}

\item{sim_reps}{The number of bootstrap samples when \code{method = "2SD"}
or \code{method = "simulation"}. Default is \code{1000}. Increasing to \code{5000}
gives a more stable interval.}
}
\value{
An S3 object of class \code{stat218_2prop_ci} containing all computed
values. You can use the following methods on the result:
\describe{
\item{\code{print(result)}}{Displays a clean summary of the interval
including both group proportions, the observed difference, SD of
sampling distribution, and the interval bounds.}
\item{\code{plot(result)}}{Shows the bootstrap or theoretical
distribution with the confidence interval shaded and labeled,
a forest plot of the interval, and a beautified 2x2 contingency
table.}
\item{\code{plot_steps(result)}}{Displays a detailed three-panel
step-by-step construction of the interval with proper fraction
notation.}
}
}
\description{
Constructs a confidence interval for the difference between two population
proportions (\eqn{\pi_1 - \pi_2}) using one of three methods: the 2SD
simulation method, a general simulation method, or a theory-based approach.

This function accepts input in \strong{two ways}:
\itemize{
\item \strong{Summary Statistics:} You already know the number of successes and
sample sizes for both groups. Pass \code{success_1}, \code{n_1}, \code{success_2},
and \code{n_2} directly. Note that \code{method = "2SD"} and
\code{method = "simulation"} are not available with summary statistics.
\item \strong{Raw Data:} You have an actual dataset loaded into R. Pass \code{formula},
\code{data}, and \code{success_level} instead, and the function counts successes
for you automatically. All three methods are available with raw data.
}
}
\details{
\subsection{The Core Idea}{

A two-sample proportion confidence interval gives a range of plausible
values for the true difference \eqn{\pi_1 - \pi_2}. A positive interval
suggests Group 1 has a higher proportion; a negative interval suggests
Group 2 is higher; an interval containing zero is consistent with no
difference between the two groups.
}

\subsection{The 2SD Method}{

An independent bootstrap where successes and failures from each group
are resampled with replacement separately. The difference in simulated
proportions is recorded for each repetition. The SD of those differences
estimates the SE, and the interval is point estimate \eqn{\pm 2 \times SD}.
Note that unlike \code{test_2prop} which uses a permutation approach, the CI
bootstrap resamples each group independently -- this is correct for
confidence intervals where we are not assuming the null hypothesis.
}

\subsection{Theory Method}{

Uses the \strong{unpooled} standard error (unlike the hypothesis test which
uses a pooled proportion). This is because for a confidence interval we
are not assuming the two proportions are equal:
\deqn{SE = \sqrt{\frac{\hat{p}_1(1-\hat{p}_1)}{n_1} +
\frac{\hat{p}_2(1-\hat{p}_2)}{n_2}}}
}

\subsection{Validity Conditions for Theory Method}{

All four cells in the 2x2 contingency table must have at least 10
observations:
\itemize{
\item Successes in Group 1 \eqn{\geq 10}
\item Failures in Group 1 \eqn{\geq 10}
\item Successes in Group 2 \eqn{\geq 10}
\item Failures in Group 2 \eqn{\geq 10}
}
}
}
\examples{
# --- Summary Statistics (theory, default) ---
result <- ci_2prop(
  success_1 = 45, n_1 = 100,
  success_2 = 25, n_2 = 100,
  group_names = c("Treatment", "Control"),
  method = "theory"
)
print(result)
\dontrun{
plot(result)
plot_steps(result)
}

# --- Summary Statistics (theory, 90\% CI) ---
result2 <- ci_2prop(
  success_1 = 45, n_1 = 100,
  success_2 = 25, n_2 = 100,
  group_names = c("Treatment", "Control"),
  conf_level = 0.90, method = "theory"
)
print(result2)
\dontrun{
plot(result2)
plot_steps(result2)
}

# --- Raw Data (2SD method) ---
car_data <- mtcars
car_data$transmission <- ifelse(mtcars$am == 1, "Manual", "Automatic")
car_data$engine <- ifelse(mtcars$vs == 1, "Straight", "V-shaped")
result3 <- ci_2prop(
  formula = transmission ~ engine, data = car_data,
  success_level = "Manual", method = "2SD"
)
print(result3)
\dontrun{
plot(result3)
plot_steps(result3)
}

# --- Raw Data (simulation, 90\% CI) ---
result4 <- ci_2prop(
  formula = transmission ~ engine, data = car_data,
  success_level = "Manual", conf_level = 0.90,
  method = "simulation"
)
print(result4)
\dontrun{
plot(result4)
plot_steps(result4)
}

# --- Raw Data (theory) ---
result5 <- ci_2prop(
  formula = transmission ~ engine, data = car_data,
  success_level = "Manual", method = "theory"
)
print(result5)
\dontrun{
plot(result5)
plot_steps(result5)
}

}
