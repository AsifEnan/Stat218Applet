% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_1prop.R
\name{ci_1prop}
\alias{ci_1prop}
\title{1-Sample Proportion Confidence Interval}
\usage{
ci_1prop(
  successes = NULL,
  n = NULL,
  formula = NULL,
  data = NULL,
  success_level = NULL,
  conf_level = 0.95,
  method = "2SD",
  sim_reps = 1000
)
}
\arguments{
\item{successes}{A whole number representing the count of successes
observed in your sample. For example, if 42 out of 80 students passed,
\code{successes = 42}.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{n}{A whole number representing the total sample size.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{formula}{A one-sided formula of the form \code{~ variable} identifying
the categorical variable in your dataset. For example,
\code{formula = ~ Passed}.
\strong{Only used when NOT providing \code{successes} and \code{n}.}}

\item{data}{A data frame (loaded from a \code{.csv}, \code{.txt}, or \code{.xlsx} file)
containing the variable named in \code{formula}.
\strong{Only used when NOT providing summary statistics.}}

\item{success_level}{A character string specifying which value of your
categorical variable counts as a "success". For example, if your
variable contains \code{"Yes"} and \code{"No"}, use \code{success_level = "Yes"}.
\strong{Required when using \code{formula} and \code{data}.}}

\item{conf_level}{The desired confidence level as a decimal. Default is
\code{0.95} for a 95\% confidence interval. Common choices are \code{0.90},
\code{0.95}, and \code{0.99}. Note that \code{method = "2SD"} only works at \code{0.95}.}

\item{method}{The method used to construct the confidence interval.
Must be one of:
\describe{
\item{\code{"2SD"}}{(default) The 2SD Simulation Method. A bootstrap
distribution is generated centered at \code{p_hat} by drawing repeated
samples of size \code{n} from a population with proportion \code{p_hat}.
The interval is \eqn{\hat{p} \pm 2 \times SD(\hat{p})}. Only
valid for 95\% confidence. Use \code{method = "simulation"} for other
confidence levels.}
\item{\code{"simulation"}}{The Bootstrap Simulation Method. Same
bootstrap as 2SD, but uses the middle \code{conf_level}\% of the
simulated distribution to find bounds directly. Works for any
confidence level.}
\item{\code{"theory"}}{The Theory-Based Method. Uses the formula
\eqn{SE = \sqrt{\hat{p}(1-\hat{p})/n}} and the appropriate
Z* multiplier. Works for any confidence level. Requires validity
conditions: at least 10 expected successes and 10 expected
failures in the sample.}
}}

\item{sim_reps}{The number of bootstrap samples when \code{method = "2SD"} or
\code{method = "simulation"}. Default is \code{1000}. Increasing to \code{5000} gives
a more stable interval.}
}
\value{
An S3 object of class \code{stat218_1prop_ci} containing all computed
values. You can use the following methods on the result:
\describe{
\item{\code{print(result)}}{Displays a clean summary of the interval.}
\item{\code{plot(result)}}{Shows the bootstrap or theoretical distribution
with the confidence interval shaded and labeled, plus a forest
plot of the interval below. Use \code{plot(result, plot_type = "dotplot")}
for a dot plot of the bootstrap distribution (simulation methods
only).}
\item{\code{plot_steps(result)}}{Displays a detailed three-panel
step-by-step construction of the interval.}
}
}
\description{
Constructs a confidence interval for a single population proportion
(\eqn{\pi}) using one of three methods: the 2SD simulation method, a
general simulation method, or a theory-based approach.

This function accepts input in \strong{two ways}:
\itemize{
\item \strong{Summary Statistics:} You already know the number of successes and
the sample size. Pass \code{successes} and \code{n} directly.
\item \strong{Raw Data:} You have an actual dataset loaded into R. Pass \code{formula},
\code{data}, and \code{success_level} instead, and the function counts the
successes for you automatically.
}
}
\details{
\subsection{The Core Idea}{

A confidence interval gives a range of plausible values for the true
population proportion \eqn{\pi}. Rather than reporting just \eqn{\hat{p}},
we acknowledge sampling variability and say: \emph{"We are X\% confident the
true proportion falls somewhere in this range."}
}

\subsection{The 2SD Method}{

The book's primary simulation-based approach for 95\% confidence intervals.
We simulate the sampling distribution of \eqn{\hat{p}} by drawing many
bootstrap samples from a population where the true proportion equals our
observed \eqn{\hat{p}}. The interval is constructed as
\eqn{\hat{p} \pm 2 \times SD} of that distribution. The multiplier of 2
is an approximation of the true 1.96 used in theory.
}

\subsection{Theory Method}{

Uses the formula \eqn{SE = \sqrt{\hat{p}(1-\hat{p})/n}} and the
appropriate Z* multiplier. Requires:
\itemize{
\item At least 10 successes in the sample (\eqn{n \cdot \hat{p} \geq 10})
\item At least 10 failures in the sample (\eqn{n \cdot (1-\hat{p}) \geq 10})
}
}
}
\note{
To display the bootstrap distribution as a dotplot instead of a
histogram, use \code{plot(result, plot_type = "dotplot")} after
running this function. Only available for \code{method = "2SD"} or
\code{method = "simulation"}.
}
\examples{
# --- Summary Statistics (2SD method, default) ---
result <- ci_1prop(successes = 42, n = 80)
print(result)
\dontrun{
plot(result)
plot_steps(result)
}

# --- Summary Statistics (theory, 95\% CI) ---
result2 <- ci_1prop(successes = 42, n = 80,
                    conf_level = 0.95, method = "theory")
print(result2)
\dontrun{
plot(result2)
plot_steps(result2)
}

# --- Summary Statistics (simulation, 90\% CI) ---
result3 <- ci_1prop(successes = 42, n = 80,
                    conf_level = 0.90, method = "simulation")
print(result3)
\dontrun{
plot(result3)
plot_steps(result3)
}

# --- Raw Data (2SD method) ---
car_data <- mtcars
car_data$transmission <- ifelse(mtcars$am == 1, "Manual", "Automatic")
result4 <- ci_1prop(formula = ~ transmission, data = car_data,
                    success_level = "Manual", method = "2SD")
print(result4)
\dontrun{
plot(result4)
plot_steps(result4)
}

# --- Raw Data (theory) ---
result5 <- ci_1prop(formula = ~ transmission, data = car_data,
                    success_level = "Manual", method = "theory")
print(result5)
\dontrun{
plot(result5)
plot_steps(result5)
}

}
