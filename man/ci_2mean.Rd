% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_2mean.R
\name{ci_2mean}
\alias{ci_2mean}
\title{2-Sample Means Confidence Interval}
\usage{
ci_2mean(
  x_bar_1 = NULL,
  sd_1 = NULL,
  n_1 = NULL,
  x_bar_2 = NULL,
  sd_2 = NULL,
  n_2 = NULL,
  group_names = NULL,
  sd_type = "sample",
  formula = NULL,
  data = NULL,
  conf_level = 0.95,
  method = "2SD",
  sim_reps = 1000
)
}
\arguments{
\item{x_bar_1}{The observed sample mean of Group 1.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{sd_1}{The standard deviation of Group 1. Whether this is a
population or sample SD is determined by \code{sd_type}.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{n_1}{The sample size of Group 1.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{x_bar_2}{The observed sample mean of Group 2.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{sd_2}{The standard deviation of Group 2.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{n_2}{The sample size of Group 2.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{group_names}{A character vector of length 2 giving meaningful names
to the two groups. For example,
\code{group_names = c("Treatment", "Control")}.
Defaults to \code{c("Group 1", "Group 2")}. When using raw data, group
names are extracted automatically from the grouping variable.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{sd_type}{A character string specifying whether the standard
deviations are from the population or the sample. Must be one of:
\describe{
\item{\code{"sample"}}{(default) The SDs were calculated from the sample
data. When \code{method = "theory"}, a T-interval is used with degrees
of freedom computed automatically via the Satterthwaite
approximation.}
\item{\code{"population"}}{The true population SDs (\eqn{\sigma}) are
known. When \code{method = "theory"}, a Z-interval is used.}
}
When raw data is provided, this is always set to \code{"sample"}
automatically.}

\item{formula}{A two-sided formula of the form \code{Response ~ Group}
identifying the numeric response variable and the grouping variable
in your dataset. For example, \code{formula = Score ~ Method}.
\strong{Only used when NOT providing summary statistics.}}

\item{data}{A data frame (loaded from a \code{.csv}, \code{.txt}, or \code{.xlsx} file)
containing the variables named in \code{formula}.
\strong{Only used when NOT providing summary statistics.}}

\item{conf_level}{The desired confidence level as a decimal. Default is
\code{0.95} for a 95\% confidence interval. Common choices are \code{0.90},
\code{0.95}, and \code{0.99}. Note that \code{method = "2SD"} only works at \code{0.95}.}

\item{method}{The method used to construct the confidence interval.
Must be one of:
\describe{
\item{\code{"2SD"}}{(default) The 2SD Simulation Method. A parametric
bootstrap distribution is generated for the difference in means by independently drawing samples from both groups.
This method is only valid for 95\% confidence intervals.The interval
is \eqn{(\bar{x}_1 - \bar{x}_2) \pm 2 \times SD}. If you
need a different confidence level, use \code{method = "simulation"}.}
\item{\code{"simulation"}}{The Bootstrap Simulation Method. Same
parametric bootstrap as 2SD, but uses the middle \code{conf_level}\% of
the simulated distribution to find the bounds. Works for any
confidence level.}
\item{\code{"theory"}}{The Theory-Based Method. Uses the unpooled
standard error formula and the appropriate Z* or t* multiplier.
Works for any confidence level and with both summary statistics
and raw data.}
}}

\item{sim_reps}{The number of bootstrap samples when \code{method = "2SD"}
or \code{method = "simulation"}. Default is \code{1000}.}

\item{plot_type}{The type of plot for the bootstrap distribution when
\code{method = "2SD"} or \code{method = "simulation"}. Must be one of:
\describe{
\item{\code{"distribution"}}{(default) Shows the null bootstrap
distribution with shaded confidence region.}
\item{\code{"boxplot"}}{Side-by-side boxplots of the two groups.
Only available when raw data was provided.}
}}
}
\value{
An S3 object of class \code{stat218_2mean_ci} containing all computed
values. You can use the following methods on the result:
\describe{
\item{\code{print(result)}}{Displays a clean summary of the interval.}
\item{\code{plot(result)}}{Default shows the bootstrap or theoretical
distribution with the confidence interval shaded and labeled,
plus a forest plot below. Use
\code{plot(result, plot_type = "boxplot")} for side-by-side boxplots
(raw data only).}
\item{\code{plot_steps(result)}}{Displays a detailed three-panel
step-by-step construction of the interval.}
}
}
\description{
Constructs a confidence interval for the difference between two population
means (\eqn{\mu_1 - \mu_2}) using one of three methods: the 2SD simulation
method, a general simulation method, or a theory-based approach.

This function accepts input in \strong{two ways}:
\itemize{
\item \strong{Summary Statistics:} You already know the sample mean, standard
deviation, and sample size for both groups. Pass \code{x_bar_1}, \code{sd_1},
\code{n_1}, \code{x_bar_2}, \code{sd_2}, and \code{n_2} directly. Note that
\code{method = "2SD"} and \code{method = "simulation"} are not available with
summary statistics — they require the individual data values.
\item \strong{Raw Data:} You have an actual dataset loaded into R. Pass \code{formula}
and \code{data} instead, and the function computes all summary statistics
automatically. All three methods are available with raw data.
}
}
\details{
\subsection{The Core Idea}{

A two-sample mean confidence interval gives a range of plausible values
for the true difference between two population means \eqn{\mu_1 - \mu_2}.
A positive interval suggests Group 1 tends to be higher; a negative
interval suggests Group 2 tends to be higher; an interval containing
zero is consistent with no difference.
}

\subsection{The 2SD Method}{

A parametric bootstrap where repeated samples of size \code{n_1} are drawn
from Normal(\eqn{\bar{x}_1}, s_1) and size \code{n_2} from
Normal(\eqn{\bar{x}_2}, s_2). The difference in sample means is recorded
for each repetition. The SD of those differences estimates the SE, and
the interval is point estimate \eqn{\pm 2 \times SD}.
}

\subsection{Theory Method}{

Uses the unpooled standard error:
\deqn{SE = \sqrt{\frac{s_1^2}{n_1} + \frac{s_2^2}{n_2}}}
The degrees of freedom are computed automatically using the
Satterthwaite approximation and handled in the background.
}

\subsection{Validity Conditions}{

The theory-based interval is most reliable when:
\itemize{
\item Both groups have at least 20 observations, \strong{or}
\item The distributions within both groups are roughly symmetric.
}
}
}
\examples{
# --- Summary Statistics (theory T-interval, default) ---
result <- ci_2mean(
  x_bar_1 = 78.3, sd_1 = 9.1, n_1 = 35,
  x_bar_2 = 73.5, sd_2 = 8.4, n_2 = 32,
  group_names = c("New Method", "Traditional"),
  method = "theory"
)
print(result)
plot(result)
plot_steps(result)

# --- Summary Statistics (theory, 90\% CI) ---
result2 <- ci_2mean(
  x_bar_1 = 78.3, sd_1 = 9.1, n_1 = 35,
  x_bar_2 = 73.5, sd_2 = 8.4, n_2 = 32,
  group_names = c("New Method", "Traditional"),
  conf_level = 0.90, method = "theory"
)
print(result2)
plot(result2)
plot_steps(result2)

# --- Raw Data (2SD method) ---
car_data <- mtcars
car_data$transmission <- ifelse(mtcars$am == 1, "Manual", "Automatic")
result3 <- ci_2mean(
  formula = mpg ~ transmission, data = car_data,
  method = "2SD"
)
print(result3)
plot(result3)
plot(result3, plot_type = "boxplot")
plot_steps(result3)

# --- Raw Data (simulation, 90\% CI) ---
result4 <- ci_2mean(
  formula = mpg ~ transmission, data = car_data,
  conf_level = 0.90, method = "simulation"
)
print(result4)
plot(result4)
plot_steps(result4)

# --- Raw Data (theory T-interval) ---
result5 <- ci_2mean(
  formula = mpg ~ transmission, data = car_data,
  method = "theory"
)
print(result5)
plot(result5)
plot(result5, plot_type = "boxplot")
plot_steps(result5)

}
