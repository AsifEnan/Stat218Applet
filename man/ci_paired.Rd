% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_paired.R
\name{ci_paired}
\alias{ci_paired}
\title{Paired Data Confidence Interval}
\usage{
ci_paired(
  x_bar_d = NULL,
  sd_d = NULL,
  n_d = NULL,
  formula = NULL,
  data = NULL,
  name = "Differences",
  conf_level = 0.95,
  method = "2SD",
  sim_reps = 1000
)
}
\arguments{
\item{x_bar_d}{The observed sample mean of the differences.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{sd_d}{The standard deviation of the differences across all pairs.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{n_d}{A whole number representing the total number of pairs.
\strong{Only used when NOT providing \code{formula} and \code{data}.}}

\item{formula}{A formula specifying the data structure. Two formats
are accepted:
\describe{
\item{\code{~ Differences}}{A one-sided formula when your dataset contains
a single column of pre-computed differences.}
\item{\code{After ~ Before}}{A two-sided formula when your dataset has two
separate measurement columns. Differences are computed as
After minus Before.}
}
\strong{Only used when NOT providing \code{x_bar_d}, \code{sd_d}, and \code{n_d}.}}

\item{data}{A data frame (loaded from a \code{.csv}, \code{.txt}, or \code{.xlsx} file)
containing the variable(s) named in \code{formula}.
\strong{Only used when NOT providing summary statistics.}}

\item{name}{A short descriptive label for what the differences represent.
Used in plot axis labels. For example, \code{name = "After - Before"}.
Defaults to \code{"Differences"}.}

\item{conf_level}{The desired confidence level as a decimal. Default is
\code{0.95} for a 95\% confidence interval. Common choices are \code{0.90},
\code{0.95}, and \code{0.99}. Note that \code{method = "2SD"} only works at \code{0.95}.}

\item{method}{The method used to construct the confidence interval.
Must be one of:
\describe{
\item{\code{"2SD"}}{(default) The 2SD Simulation Method. Uses a
sign-flipping bootstrap centered at the observed mean difference.
For each repetition, a coin is flipped for every pair — heads
flips the sign of that pair's difference, tails leaves it as is.
The interval is \eqn{\bar{x}_d \pm 2 \times SD}.
Can only be used with 95\%
confidence interval. Requires raw data.}
\item{\code{"simulation"}}{The Bootstrap Simulation Method. Same
sign-flipping bootstrap as 2SD, but uses the middle \code{conf_level}\%
of the simulated distribution to find bounds. Works for any
confidence level. Requires raw data.}
\item{\code{"theory"}}{The Theory-Based T-Interval. Uses the formula
SE = s_d / sqrt(n_d) and the appropriate t* multiplier. Works for
any confidence level and with both summary statistics and raw data.
Degrees of freedom = n_d - 1, handled automatically.}
}}

\item{sim_reps}{The number of sign-flipping repetitions when
\code{method = "2SD"} or \code{method = "simulation"}. Default is \code{1000}.}
}
\value{
An S3 object of class \code{stat218_ci_paired} containing all computed
values. You can use the following methods on the result:
\describe{
\item{\code{print(result)}}{Displays a clean summary of the interval.}
\item{\code{plot(result)}}{Shows the bootstrap or theoretical distribution
with the confidence interval shaded and labeled, plus a forest
plot of the interval below.}
\item{\code{plot_steps(result)}}{Displays a detailed three-panel
step-by-step construction of the interval with proper fraction
notation.}
}
}
\description{
Constructs a confidence interval for the true mean difference between
paired measurements (\eqn{\mu_d}) using one of three methods: the 2SD
simulation method, a general simulation method, or a theory-based
T-interval.

Paired data arises when each subject contributes \strong{two related
measurements} — for example, a before/after reading, a left/right
measurement, or two treatments applied to the same subject. Because
the two measurements within each pair are linked, we work with the
\strong{differences} rather than treating the two groups independently.

This function accepts input in \strong{three ways}:
\itemize{
\item \strong{Summary Statistics:} You already know the mean difference, standard
deviation of differences, and number of pairs. Pass \code{x_bar_d}, \code{sd_d},
and \code{n_d} directly. Note that \code{method = "2SD"} and
\code{method = "simulation"} are not available with summary statistics.
\item \strong{Single Differences Column:} You have a dataset with one column
containing the pre-computed differences. Use \code{formula = ~ Differences}
with your data frame.
\item \strong{Two Columns (Before/After):} You have a dataset with two separate
columns — one for each measurement. Use \code{formula = After ~ Before}
and the function computes differences as After minus Before for each
pair automatically.
}
}
\details{
\subsection{The Core Idea}{

A paired confidence interval gives a range of plausible values for the
true mean difference \eqn{\mu_d}. An interval entirely above zero
suggests the "After" measurement tends to be higher; entirely below
zero suggests it tends to be lower; an interval containing zero is
consistent with no systematic difference.
}

\subsection{Why Paired and Not Two-Sample?}{

Because the two measurements within each pair are linked to the same
subject, treating them as independent groups ignores that connection.
By working with differences, we control for subject-to-subject
variability and gain statistical power.
}

\subsection{The 2SD Method (Sign-Flipping Bootstrap)}{

Under the null hypothesis for a CI, we center the bootstrap at the
observed mean difference. For each repetition, a coin is flipped for
every pair — heads flips the sign of that pair's difference (simulating
the idea that the two measurements are interchangeable), tails leaves it
as is. The SD of the resulting distribution estimates the SE, and the
interval is \eqn{\bar{x}_d \pm 2 \times SD}.
}

\subsection{Theory Method}{

Uses the formula SE = s_d / sqrt(n_d) with the t* multiplier from the
T-distribution with df = n_d - 1. This is the most reliable approach
when validity conditions are met.
}

\subsection{Validity Conditions}{

The theory-based T-interval is most reliable when:
\itemize{
\item The number of pairs \eqn{n_d \geq 20}, \strong{or}
\item The distribution of the differences is roughly symmetric.
}
}
}
\examples{
# --- Summary Statistics (theory, default) ---
result <- ci_paired(x_bar_d = 4.2, sd_d = 6.8, n_d = 25,
                    name = "Post - Pre", method = "theory")
print(result)
plot(result)
plot_steps(result)

# --- Summary Statistics (theory, 90\% CI) ---
result2 <- ci_paired(x_bar_d = 4.2, sd_d = 6.8, n_d = 25,
                     name = "Post - Pre", conf_level = 0.90,
                     method = "theory")
print(result2)
plot(result2)
plot_steps(result2)

# --- Single Differences Column (2SD method) ---
result3 <- ci_paired(formula = ~ extra,
                     data = sleep[sleep$group == 1, ],
                     name = "Extra Sleep (hours)", method = "2SD")
print(result3)
plot(result3)
plot_steps(result3)

# --- Two Columns Before/After (simulation, 90\% CI) ---
study_data <- data.frame(
  Before = c(72, 68, 75, 80, 65, 70, 78, 82, 69, 74),
  After  = c(78, 72, 80, 84, 70, 75, 82, 85, 73, 79)
)
result4 <- ci_paired(formula = After ~ Before, data = study_data,
                     name = "After - Before", conf_level = 0.90,
                     method = "simulation")
print(result4)
plot(result4)
plot_steps(result4)

# --- Two Columns Before/After (theory) ---
result5 <- ci_paired(formula = After ~ Before, data = study_data,
                     name = "After - Before", method = "theory")
print(result5)
plot(result5)
plot_steps(result5)

}
